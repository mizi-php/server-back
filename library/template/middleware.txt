[#PHP]

namespace [#namespace];

use Mizi\Middleware\Intarfece\InterfaceMiddleware;

/** Middleware [#name] */
abstract class [#class] implements Middleware
{
    static function run(\Closure $next): mixed
    {
        return $next();
    }
}